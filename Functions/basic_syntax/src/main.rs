fn add(a: i32, b:i32) -> i32 {
    return a + b;
}
// fn = function
// a: i32 = parameter with type
// -> i32 = return type
// return is optional if i endwith an expression

fn add(a: i32, b: i32) -> i32 {
    a + b // no semicolon = implicit return
}